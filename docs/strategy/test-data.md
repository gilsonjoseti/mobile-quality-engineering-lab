# Test Data Strategy

- Use dedicated fixtures and factories instead of hard-coded values inside test logic
- Keep realistic but non-production data
- Separate user roles, permissions, and state transitions
- Do not commit real credentials or tokens
- Validate negative and boundary cases in dedicated data sets
